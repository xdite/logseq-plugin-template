import '@logseq/libs'
import { saveAudioToAssets } from './utils'
import { settingsSchema } from './settings'

/**
 * main entry
 */
async function main() {

}

// bootstrap
logseq.ready(main).catch(console.error)
