{
  "name": "logseq-plugin-template",
  "version": "0.0.1",
  "description": "Logseq Plugin Template",
  "author": "xdite",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild index.ts --bundle --outfile=dist/index.js --format=iife --watch",
    "build": "esbuild index.ts --bundle --outfile=dist/index.js --format=iife --minify"
  },
  "devDependencies": {
    "@logseq/libs": "^0.0.17",
    "@types/uuid": "^9.0.1",
    "esbuild": "^0.17.19",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "logseq": {
    "main": "dist/index.html",
    "id": "logseq-plugin-template"
  }
}
